Hey guys, the two main algorithms that we have seen so far to determine the color of a pixel, phong lighting and the more advanced physically based rendering, are mostly about calculating the amount of lighting that hits the pixel and combining it with some texture. However, in the real world we also have phenomena such as stuff being reflected off an object as well as when light changes direction as it passes through some medium. Today we will explore these two effects - reflection and refraction. In computer graphics you may also encounter them under the umbrella name environment mapping. Now to implement it we need some math and geometry and this brings me to todays sponsor - which is brilliant. Brilliant is a fantastic platform packed with expertly crafted courses that make complex topics much easier to grasp through interactive, engaging lessons. And you know the best part is that they offer a 30-day free trial, giving you full access to their entire content library so you can explore everything before committing. If you're looking to build a strong foundation in math, Brilliant is the perfect place to start. Their math courses will help you refresh high school topics such as equations, geometry, algebra and functions and will take you all the way to college with Calculus, linear algebra and more. Brilliant’s learning approach is highly visual and hands-on, so instead of just watching tutorials, you're actively engaging through quizzes, challenges, and interactive exercises. This method makes learning much more effective and fun. So whether you’re just starting out or diving into advanced topics like calculus, Brilliant’s intuitive step-by-step teaching method makes learning math far more approachable. If you want to try it out, check the link in the video description or scan the QR code onscreen to begin your 30-day free trial. You’ll also get 20% off an annual premium subscription. A big thank you to Brilliant for sponsoring this video! So let’s start with reflection which is the simpler technique. The idea is to draw a ray from the viewer to the pixel, bounce it to the other side where it should eventually hit something in the environment and use the color from there on our pixel. This may sound a bit like ray tracing which is much more complex, but instead of actually hitting the environment or the objects in it, we will use a cubemap texture. We’ve already seen the use of cubemaps to render a skybox around the scene. This was done by rendering a box around the viewer and sampling the cubemap using a vector that goes from the viewer to any pixel being rasterized on the box. The skybox usually has its own shader program which is much simpler than the programs that we use for the objects in the scene. We can bind the same cubemap to our main lighting shader, calculate the vector which bounces from the pixel and sample the cubemap using that vector. The result can override entirely or mixed with the original pixel color. To implement reflection in OpenGL we use a built in GLSL function reflect which takes two vectors - an incident vector which represents the view direction and a normal vector. The function returns the vector which is reflected from the surface over the normal. You can manually calculate this vector using the following formula. This is basically what the reflect function does. This is the main function that calculates Phong lighting it takes the normal vector as a parameter the normal can come directly from the vertex shader after being interpolated by the rasterizer and it can also come from the normal map using bump mapping at this point we don't really care so we start by calculating the total light which affects the pixel and then we sample some color from the texture there are a few cases here and finally we multiply them together to get the final color for the pixel now previously this would simply be returned to the caller but now we have a uniform boolean flag which indicates where the reflection and refraction are enabled and this allows us to turn the two effects on and off from the application side the function which calculates refraction and refraction is called ApplyReRefract it takes the final color from here and the normal vector and it overrides the final color and then again we return it to the caller so this is kind of like the highlevel integration of refraction and refraction to the shader program here and we can now focus on this function ApplyRefRefract which is defined up here okay takes the final color and the normal and it starts by calculating the view vector the vector that goes from the camera to the pixel okay this is the vector that we need to bounce or reflect over the normal so as usual with vectors we need to subtract the starting position from the end position and we have a uniform variable for the camera position in the world and this guy WorldPos0 is the input attribute from the vertex shader okay these are the variables that the vertex shader sends us everything is interpolated by the rasterizer and we have the world position here so we do the subtraction and we normalize the result next we use the internal function that we talked about reflect to reflect the camera to pixel vector over the normal and we normalize the result and call this the reflection direction now as I'm recording this I started thinking that we actually don't need the first normalization or at least we only need one of them but I'm going to keep the code as is for now and check this later you can tell me what you think in the comments below next we use the reflection vector to sample from the cube map texture so gCubemapTexture is a uniform variable of the samplerCubeType and this is the same texture that we use in the skybox technique so it was added here and we sample from it take the RGB value extend this to a 4D vector by adding one in the alpha and this is our color reflect our reflection color and finally we mix the final color with a reflection color based on gReflectionFactor which is a uniform variable that goes between 0 and one when it is zero of course we get the final color and when it is one we get the reflection color and everything in between is interpolated by the mix function so we override the final color and we return it to the caller so now let's try to run this you can press spacebar to get the GUI and then enable reflection refraction and as you can see the cube map is reflected from the model we can play with the reflection factor so when it is zero we get no reflection and when it is one the reflection color takes over so this is really like a mirror but it seems to be too extreme